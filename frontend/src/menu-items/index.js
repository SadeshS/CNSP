// project-imports
import predictions from './predictions';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [predictions]
};

export default menuItems;
